{
  "name": "worldtime",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "preview": "astro preview",
    "cf:types": "wrangler types",
    "cf:check": "wrangler deploy --dry-run --config ./wrangler.jsonc --cwd .",
    "deploy:cloudflare": "npm run build && wrangler deploy --config ./wrangler.jsonc --cwd .",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/preact": "^6.0.1",
    "@tailwindcss/vite": "^4.3.2",
    "astro": "7.2.0",
    "preact": "^10.29.7",
    "suncalc": "^2.0.1",
    "tailwindcss": "^4.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "@types/node": "^24.0.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.115.0"
  },
  "overrides": {
    "fast-uri": "3.1.6",
    "js-yaml": "4.3.1",
    "nanoid": "3.3.18",
    "postcss": "8.5.26",
    "undici": "7.29.0"
  }
}
